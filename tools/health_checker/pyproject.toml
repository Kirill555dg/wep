[project]
name = "health-checker"
version = "0.1.0"
description = "Health checker for WEP infrastructure"
requires-python = ">=3.12"
dependencies = [
    "click>=8.1.0,<9.0.0",
    "psycopg[binary]>=3.2.0,<4.0.0",
    "minio>=7.2.0,<8.0.0",
]

[project.scripts]
health-checker = "health_checker.__main__:main"
