# Frontend environment variables - copy to .env and edit for your setup.
#
# For Docker / nginx (recommended): leave empty string so requests go
# through the same origin (nginx proxies /api → backend).
VITE_API_URL=

# For local development without Docker (CORS needed in backend):
# VITE_API_URL=http://localhost:8023/api

VITE_APP_TITLE=Test Constructor
